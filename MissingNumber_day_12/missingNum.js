// Brute force method


function checkTheMissingNum(arr){

    const n = arr.length

    for(let i=0; i<=n; i++){
        if(!arr.includes(num)){
            return numm
        }
    }
    return -1
}